#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_autoprefixer@9.5.1@autoprefixer/bin/autoprefixer" "$@"
  ret=$?
else 
  node  "$basedir/../_autoprefixer@9.5.1@autoprefixer/bin/autoprefixer" "$@"
  ret=$?
fi
exit $ret
